const imageBase = css`
  width: 32px;
  height: 32px;
  border-radius: 50%;
`

const largerImage = css`
  width: 48px;
  height: 32px;
`

const Avatar = styled.img`
  ${imageBase};
  ${largerImage};
`

render(<Avatar src={logoUrl} />, mountNode)
